#include <stdio.h>
#include <stdlib.h>
#include "lib.h"

int main(){
    int k = 10;

    printf("%i", multi(k));

    /*char str[101], str1[51], str2[51];

    puts("Digite uma sequencia de caracteres");
    scanf("%50s", str1);

    puts("Digite outra sequencia de caracteres");
    scanf("%50s", str2);

    copia(str1, str);
    concat(str, str2);
    printf("%s\n", str);
    printf("Comprimento das strings concatenadas: %i", comprimento(str));*/
}
